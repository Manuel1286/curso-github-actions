name: requirements-project

on:
  pull_request:
    branches:
      - staging
      - master

jobs:
  requirements:
    uses: bancodebogota/bbog-dig-do-testing-pipeline/.github/workflows/requirements_frontend.yml@v7
    with:
      project-name: bbog-bm-automated-testing
      project-key:  bbog-bm-automated-testing
      language: java
      sonar-project-properties-file: 'sonar-project-custom.properties'
    secrets: inherit

